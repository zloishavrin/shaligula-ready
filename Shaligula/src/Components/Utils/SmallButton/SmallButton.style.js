import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#434343',
        borderRadius: 10,
        marginLeft: 10
    },
    image: {
        width: 40,
        height: 40
    }
})